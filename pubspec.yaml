name: tomamoni
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.1.0
  syncfusion_flutter_gauges: ^22.1.35
  symbol_rest_client: ^1.0.1
  symbol_sdk: ^1.0.19
  flutter_riverpod: ^2.5.1
  cupertino_icons: ^1.0.6
  fl_chart: ^0.68.0
  shared_preferences: ^2.2.3
  logger: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
